# Shared hall database (Neon / any Postgres). Required for a real class
# across phones. Leave unset for a single-machine try-out (embedded PGLite).
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require

# Must stay false. This classroom uses class/teacher passwords, not Grok accounts.
VITE_AUTH_ENABLED=false
